package com.se.dummytempmonitorbackend.controller;

/*
import com.se.dummytempmonitorbackend.entityes.SensorMock;
import com.se.dummytempmonitorbackend.service.SensorMockRepositary;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@CrossOrigin
@RestController
@RequestMapping("/")
public class SensorMockController {

    @Autowired
    SensorMockRepositary sensorMockRepositary;

   @PostMapping("/getreaddatas")
    public ResponseEntity<SensorMock> GetReadDatas(@RequestBody SensorMock sensorMock){
         System.out.println("okay");
     SensorMock sensorMock1 =  sensorMockRepositary.save(sensorMock);
       if(sensorMock1!=null){
           return ResponseEntity.ok(sensorMock1);
       }
       return null;
   }


}
*/